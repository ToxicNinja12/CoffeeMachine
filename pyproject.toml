[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "coffee-machine"
version = "0.0.3"
dependencies = [
    "rich"
]

[project.scripts]
coffee = "coffee_machine.main:main"